yum install python-devel gcc -y

mkdir /opt/dnsreroute
cd /opt/dnsreroute
### Download app bundle and un-pack - only files needed are requirements.txt, wsgi.py, redirectorApp.py, and flaskapp.cfg

virtualenv redirector
. redirector/bin/activate

pip install -r requirements.txt
export OPENSHIFT_MONGODB_DB_URL=ds139425.mlab.com
export OPENSHIFT_MONGODB_DB_USERNAME=dnsreroute-admin
export OPENSHIFT_MONGODB_DB_PASSWORD=Qcs7WjA6Ktd2
export OPENSHIFT_MONGODB_DB_PORT=39425
chmod +x *.py

gunicorn -b 0.0.0.0:80 -k eventlet --workers=3 wsgi:app



# contents of wsgi.py:
# from redirectorApp import app
#
# if __name__ == "__main__":
#     app.run()
